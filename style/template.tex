\documentclass[12pt]{article}

\usepackage{sbc-template}
\usepackage[utf8]{inputenc}
\usepackage[brazil]{babel}
\usepackage{longtable,booktabs,array}
\usepackage{graphicx}
\usepackage{url}

$if(graphics)$
\usepackage{graphicx}
$endif$

$if(highlighting-macros)$
$highlighting-macros$
$endif$

$for(header-includes)$
$header-includes$
$endfor$

\sloppy

\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}
\providecommand{\hypertarget}[2]{#2}
\providecommand{\hyperlink}[2]{#2}

\begin{document}

$if(title)$
\title{$title$}
$endif$

$if(author)$
\author{$for(author)$$author.name$\inst{$author.inst$}$sep$, $endfor$}
$endif$

$if(addresses)$
\address{$for(addresses)$
$if(addresses.inst)$\inst{$addresses.inst$}$endif$$for(addresses.lines)$$addresses.lines$$sep$\\
$endfor$%
$if(addresses.email)$\\[-6pt]\email{$addresses.email$}$endif$
$sep$
\nextinstitute
$endfor$}
$endif$

$if(title)$
\maketitle
$endif$

$if(abstract)$
\begin{abstract}
$abstract$
\end{abstract}
$endif$

$if(resumo)$
\begin{resumo}
$resumo$
\end{resumo}
$endif$

$body$

\bibliographystyle{sbc}
\bibliography{references}

\end{document}
